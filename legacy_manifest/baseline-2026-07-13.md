# SMR MVP Refactor Safety Baseline — 2026-07-13

## Repository identity

- Repository: `D:\李少博的文件\TH_Capital_二级市场\th_capital_stock`
- Branch: `main`
- HEAD: `01ac6a948a963cbc8ac0aa54cae57aa989163b25`
- Upstream state: `main...origin/main [ahead 1]`
- Existing worktrees at inspection time: one, the primary repository worktree

## Working-tree state

Before restoring the repository ignore rules:

- Modified tracked entries: 21
- Untracked entries reported by concise status: 129
- Known untracked credential path: `config/ifind_refresh_token.txt`

After restoring the union of the repository safety rules and useful Vite/editor rules:

- Modified tracked entries: 21
- Untracked entries reported by concise status: 78
- Credential path is ignored by `config/*token*.txt`
- Runtime database, logs, Python caches, Agent workspaces, generated research output and Node build output are ignored

## Safety decisions already applied

1. No untracked file was deleted, moved, stashed or overwritten.
2. No tracked business file was reverted.
3. `.gitignore` was restored by merging the previous repository rules with useful rules from the current Vite-style file.
4. The iFinD refresh token file was not read.
5. The token file remains in place temporarily because current code and `config/phase189_ifind_capability_probe.json` still support that fallback path. Moving it before confirming an environment variable would break the current iFinD chain.
6. Future runtime code must prefer `IFIND_REFRESH_TOKEN`; removal of the file fallback requires a separate verified change.

## Critical migration concern

The current Web application is not represented in HEAD. At baseline time the following are untracked:

- `api/`
- `src/`
- `public/`
- `package.json`
- `package-lock.json`
- `index.html`
- TypeScript, Vite, Tailwind, PostCSS and ESLint configuration

Creating a new worktree directly from HEAD would therefore omit the existing Web application. Do not begin implementation in a HEAD-only worktree until these assets are either:

1. reviewed and committed as a safety snapshot, or
2. explicitly selected and copied into the isolated worktree with a recorded manifest.

Recommended path: create a dedicated safety snapshot branch containing reviewed source/config changes while excluding secrets, generated data, caches, logs and temporary investigation scripts. Then create `refactor/personal-research-mvp` from that snapshot.

## Files that must not be auto-deleted

- Anything under `01_data/`, `02_research/`, `04_portfolio/`, `05_risk/`, `06_reports/`, `10_logs/` or `11_smr_wiki/`
- `config/ifind_refresh_token.txt`
- Unknown untracked Python/JavaScript utilities until Task 2 classifies them
- Existing modified tracked business code

## Verification evidence

```text
.gitignore:60:config/*token*.txt config/ifind_refresh_token.txt
.gitignore:66:01_data/                01_data/db/smr.db
.gitignore:44:10_logs/                 10_logs/script_runs.jsonl
.gitignore:19:__pycache__/             __pycache__/x.pyc
```

This baseline is descriptive. It does not approve deletion of any listed asset.
